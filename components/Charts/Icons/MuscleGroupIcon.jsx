import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function MuscleGroupIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="body" size={size} color={color} />;
}
