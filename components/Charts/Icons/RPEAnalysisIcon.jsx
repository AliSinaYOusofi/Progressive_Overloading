import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function RPEAnalysisIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="speedometer" size={size} color={color} />;
}
