import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function MonthlyTrendsIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="stats-chart" size={size} color={color} />;
}
