import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function VolumeProgressionIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="bar-chart" size={size} color={color} />;
}
