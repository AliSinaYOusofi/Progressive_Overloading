import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function ProgressiveOverloadIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="arrow-up-circle" size={size} color={color} />;
}
