import React from "react";
import { Ionicons } from "@expo/vector-icons";

export default function WeeklyProgressIcon({ size = 28, color = "#10B981" }) {
    return <Ionicons name="calendar" size={size} color={color} />;
}
